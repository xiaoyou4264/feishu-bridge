---
phase: 01-feishu-connectivity
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - .env
autonomous: false
requirements:
  - CONN-01
  - CONN-02
  - CONN-03
  - CONN-04
  - CONN-06
  - CARD-01

user_setup:
  - service: feishu
    why: "New independent Feishu app required per D-01 (CONN-06)"
    env_vars:
      - name: APP_ID
        source: "Feishu Open Platform (open.feishu.cn) -> Create app -> App ID"
      - name: APP_SECRET
        source: "Feishu Open Platform (open.feishu.cn) -> Create app -> App Secret"
    dashboard_config:
      - task: "Create new Feishu app (do NOT reuse cli_a92d11a974b89bcd)"
        location: "https://open.feishu.cn/app -> Create Custom App"
      - task: "Enable 'im:message' and 'im:message.receive_v1' event subscription"
        location: "App -> Event Subscriptions -> Add event"
      - task: "Enable WebSocket long connection mode"
        location: "App -> Event Subscriptions -> Request method -> WebSocket"
      - task: "Publish the app (approve if needed)"
        location: "App -> Version Management -> Publish"

must_haves:
  truths:
    - "Sending a direct message to the bot produces a thinking card reply within 3 seconds"
    - "Sending the same message twice (Feishu retry) causes the bot to respond exactly once"
    - "In a group chat the bot responds only when @mentioned"
    - "The bot reconnects after disconnection without manual restart"
    - "The process refuses to start if APP_ID or APP_SECRET are missing"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification: confirm the bot works with a real Feishu app.

Purpose: Validate all Phase 1 success criteria against a live Feishu environment. This is the only way to confirm the bot actually works — unit tests mock the SDK but cannot verify real WebSocket connection, real card rendering, or real @mention behavior.

Output: Verified working bot, ready for Phase 2 Claude integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-feishu-connectivity/01-CONTEXT.md
@.planning/phases/01-feishu-connectivity/01-01-SUMMARY.md
@.planning/phases/01-feishu-connectivity/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Create Feishu app and verify end-to-end</name>
  <files>.env</files>
  <read_first>
    .env.example (for setup instructions and required variables)
    main.py (to understand startup sequence)
  </read_first>
  <action>
    This is a human verification checkpoint. The code is already implemented and unit-tested in Plans 01 and 02.

    **What was built:**
    Complete Phase 1 Feishu Bridge: WebSocket long connection, message dedup, @mention filtering, and "thinking" card reply.

    **Pre-requisites (human action needed):**
    1. Go to https://open.feishu.cn/app and create a NEW custom app (per D-01, do NOT reuse cli_a92d11a974b89bcd)
    2. In the app settings, enable:
       - Permissions: `im:message`, `im:message:send_as_bot`
       - Event subscription: `im.message.receive_v1`
       - Request method: WebSocket (long connection mode)
    3. Publish the app and approve if needed
    4. Copy APP_ID and APP_SECRET into `/home/mi/feishu-bridge/.env`

    **Verification steps:**
    1. Start the bridge: `cd /home/mi/feishu-bridge && python main.py`
       Expected: Logs show "starting" with app_id and bot_open_id, then WS connection established

    2. **CONN-01 + CARD-01**: Send a direct message to the bot in Feishu
       Expected: "思考中" card appears within 3 seconds

    3. **CONN-02**: The same message should NOT produce a second card (dedup working)

    4. **CONN-03**: In a group chat, send a message WITHOUT @mentioning the bot
       Expected: No response

    5. **CONN-03**: In the same group chat, send a message WITH @bot
       Expected: "思考中" card appears

    6. **CONN-04**: Kill the network briefly (disconnect WiFi for 5 seconds, reconnect)
       Expected: Logs show reconnection; bot continues responding after network restore

    7. **CONN-05**: Stop the bridge, remove APP_ID from .env, restart
       Expected: Process exits with "FATAL: Missing required config" message

    8. **CONN-06**: Confirm mi-feishu MCP is still working independently (different app credentials)

    **Resume signal:** Type "approved" if all checks pass, or describe which checks failed and what behavior you observed.
  </action>
  <verify>
    <automated>cd /home/mi/feishu-bridge && python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms: DM to bot produces thinking card within 3 seconds
    - Human confirms: Duplicate message does not produce second card
    - Human confirms: Group message without @mention gets no response
    - Human confirms: Group message with @mention gets thinking card
    - Human confirms: Bot reconnects after brief network interruption
    - Human confirms: Process exits when APP_ID is missing
    - Human confirms: mi-feishu MCP still works (CONN-06 isolation)
  </acceptance_criteria>
  <done>All Phase 1 success criteria verified against live Feishu environment. Bot receives messages, deduplicates, filters, and replies with thinking card. Ready for Phase 2.</done>
</task>

</tasks>

<verification>
All Phase 1 success criteria from ROADMAP.md verified:
1. Direct message produces thinking card within 3 seconds
2. Same message twice = one response (dedup)
3. Group chat: responds only when @mentioned
4. Auto-reconnect after network drop
5. Refuses to start without required config
</verification>

<success_criteria>
- Human confirms all 8 verification steps pass
- Bot is running and responsive in production Feishu environment
- Ready for Phase 2: Claude Integration
</success_criteria>

<output>
After completion, create `.planning/phases/01-feishu-connectivity/01-03-SUMMARY.md`
</output>
