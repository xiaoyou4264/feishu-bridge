---
phase: 02-claude-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - src/cards.py
  - tests/test_config.py
  - tests/test_cards.py
  - requirements.txt
  - .env.example
autonomous: true
requirements: [CLAUDE-06, CONC-02, CLAUDE-05]

must_haves:
  truths:
    - "Config.from_env() reads CLAUDE_TIMEOUT, MAX_CONCURRENT_TASKS, ALLOWED_TOOLS from environment"
    - "Config defaults: claude_timeout=120.0, max_concurrent_tasks=5, allowed_tools=[]"
    - "update_card_content() patches an existing card with new markdown text"
    - "send_error_card() patches an existing card with red error header"
    - "claude-agent-sdk==0.1.53 is in requirements.txt"
  artifacts:
    - path: "src/config.py"
      provides: "Config with claude_timeout, max_concurrent_tasks, allowed_tools fields"
      contains: "claude_timeout"
    - path: "src/cards.py"
      provides: "update_card_content() and send_error_card() functions"
      exports: ["update_card_content", "send_error_card"]
    - path: "requirements.txt"
      provides: "claude-agent-sdk dependency"
      contains: "claude-agent-sdk"
  key_links:
    - from: "src/config.py"
      to: "environment variables"
      via: "os.environ.get()"
      pattern: "CLAUDE_TIMEOUT|MAX_CONCURRENT_TASKS|ALLOWED_TOOLS"
    - from: "src/cards.py"
      to: "lark-oapi IM API"
      via: "PatchMessageRequest or BaseRequest"
      pattern: "apatch|patch"
---

<objective>
Extend the Phase 1 foundation with Claude-specific configuration fields and card update functions.

Purpose: Plan 02 (session/worker) and Plan 03 (handler wiring) need config fields for timeout and concurrency limits, plus card functions to display Claude responses and errors. This plan provides those building blocks.

Output: Extended Config model, two new card functions (update_card_content, send_error_card), updated requirements.txt with claude-agent-sdk.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-claude-integration/02-CONTEXT.md
@.planning/phases/02-claude-integration/02-RESEARCH.md

<interfaces>
<!-- Existing code the executor must read and extend -->

From src/config.py:
```python
class Config(pydantic.BaseModel):
    app_id: str
    app_secret: str
    log_level: str = "INFO"
    working_dir: str = "."

    @classmethod
    def from_env(cls) -> "Config":
        load_dotenv()
        # reads APP_ID, APP_SECRET from os.environ
        # returns cls(app_id=..., app_secret=..., log_level=..., working_dir=...)
```

From src/cards.py:
```python
THINKING_CARD_TEMPLATE: dict = {
    "header": {"title": {"tag": "plain_text", "content": "AI 助手"}, "template": "blue"},
    "elements": [{"tag": "markdown", "content": "**正在思考中...**\n\n_请稍候_"}],
}

async def send_thinking_card(client: lark.Client, message_id: str) -> str:
    # Returns reply_message_id
async def send_unsupported_type_card(client: lark.Client, message_id: str, msg_type: str) -> None:
```

From tests/test_config.py:
```python
class TestConfigFromEnv:
    # 6 existing tests for app_id, app_secret, log_level, working_dir
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Config and requirements.txt</name>
  <files>src/config.py, tests/test_config.py, requirements.txt, .env.example</files>
  <read_first>src/config.py, tests/test_config.py, requirements.txt, .env.example</read_first>
  <behavior>
    - test_config_claude_timeout_default: Config.from_env() with no CLAUDE_TIMEOUT env var produces claude_timeout=120.0
    - test_config_claude_timeout_from_env: CLAUDE_TIMEOUT=60 produces claude_timeout=60.0
    - test_config_max_concurrent_tasks_default: No MAX_CONCURRENT_TASKS env var produces max_concurrent_tasks=5
    - test_config_max_concurrent_tasks_from_env: MAX_CONCURRENT_TASKS=10 produces max_concurrent_tasks=10
    - test_config_allowed_tools_default: No ALLOWED_TOOLS env var produces allowed_tools=[] (empty list)
    - test_config_allowed_tools_from_env: ALLOWED_TOOLS="Read,Bash,Write" produces allowed_tools=["Read","Bash","Write"]
  </behavior>
  <action>
    1. Add `claude-agent-sdk==0.1.53` to `requirements.txt` (in the main deps section, before the `# dev` comment).
    2. Add three fields to Config pydantic model (per D-11, D-16, CLAUDE-06):
       - `claude_timeout: float = 120.0`
       - `max_concurrent_tasks: int = 5`
       - `allowed_tools: list[str] = []`
    3. Update `Config.from_env()` to read:
       - `claude_timeout=float(os.environ.get("CLAUDE_TIMEOUT", "120"))`
       - `max_concurrent_tasks=int(os.environ.get("MAX_CONCURRENT_TASKS", "5"))`
       - `allowed_tools`: split `os.environ.get("ALLOWED_TOOLS", "")` by comma, filter empty strings. If ALLOWED_TOOLS is empty/unset, result is `[]`.
    4. Update `.env.example` to document CLAUDE_TIMEOUT, MAX_CONCURRENT_TASKS, ALLOWED_TOOLS with comments.
    5. Run `pip install claude-agent-sdk==0.1.53` to install the dependency.
    6. Add tests to `tests/test_config.py` in a new class `TestConfigClaudeFields` following the behavior spec above. Use monkeypatch to set/unset env vars.
  </action>
  <verify>
    <automated>cd /home/mi/feishu-bridge && python3 -m pytest tests/test_config.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "claude_timeout" src/config.py
    - grep -q "max_concurrent_tasks" src/config.py
    - grep -q "allowed_tools" src/config.py
    - grep -q "claude-agent-sdk" requirements.txt
    - grep -q "CLAUDE_TIMEOUT" .env.example
    - python3 -m pytest tests/test_config.py -x exits 0
  </acceptance_criteria>
  <done>Config model has claude_timeout (default 120.0), max_concurrent_tasks (default 5), allowed_tools (default []) fields. All config tests pass. requirements.txt includes claude-agent-sdk==0.1.53.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Card update and error functions</name>
  <files>src/cards.py, tests/test_cards.py</files>
  <read_first>src/cards.py, tests/test_cards.py</read_first>
  <behavior>
    - test_update_card_content_calls_patch: update_card_content(client, message_id, "response text") calls im.v1.message patch API with interactive card containing the text in a markdown element, header "AI 助手" blue template
    - test_update_card_content_raises_on_failure: When patch response is not successful, raises RuntimeError
    - test_send_error_card_calls_patch: send_error_card(client, message_id, "error message") patches card with red header "AI 助手" and error text in markdown element
    - test_send_error_card_includes_error_text: The patched card content contains the error message string
  </behavior>
  <action>
    1. First, verify whether `client.im.v1.message.apatch` exists in lark-oapi 1.5.3. Run: `python3 -c "import lark_oapi as lark; c=lark.Client.builder().app_id('x').app_secret('y').build(); print(dir(c.im.v1.message))"` to check.
    2. If `apatch` exists: use `PatchMessageRequest.builder()` pattern (same style as areply in existing code). If NOT: use `BaseRequest` with PUT method to `/open-apis/im/v1/messages/{message_id}` with `content` body containing interactive card JSON (same raw request pattern as `get_bot_open_id` in main.py).
    3. Implement `update_card_content(client, message_id, text)`:
       - Build card JSON: header "AI 助手" blue template, elements=[markdown with text]
       - Call patch API (apatch or BaseRequest)
       - Raise RuntimeError on failure
    4. Implement `send_error_card(client, message_id, error_text)`:
       - Build card JSON: header "AI 助手" RED template, elements=[markdown with error_text formatted as "出错了: {error_text}"]
       - Call patch API (same as above)
       - Log warning on failure but do NOT raise (best-effort, same pattern as send_unsupported_type_card)
    5. Add tests to `tests/test_cards.py` using AsyncMock to mock the lark client patch method (same pattern as existing areply mocks in that file). Follow the behavior spec above.

    IMPORTANT: The card update uses `im.v1.message.patch` (HTTP PATCH), NOT `im.v1.message.reply`. The request body has a `content` field with the card JSON string. The `message_id` is the reply_message_id returned by `send_thinking_card()` — it targets the existing thinking card for in-place update.
  </action>
  <verify>
    <automated>cd /home/mi/feishu-bridge && python3 -m pytest tests/test_cards.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "async def update_card_content" src/cards.py
    - grep -q "async def send_error_card" src/cards.py
    - grep -q "test_update_card_content" tests/test_cards.py
    - grep -q "test_send_error_card" tests/test_cards.py
    - python3 -m pytest tests/test_cards.py -x exits 0
  </acceptance_criteria>
  <done>update_card_content() patches card with Claude response text (blue header). send_error_card() patches card with error message (red header). All card tests pass including existing ones.</done>
</task>

</tasks>

<verification>
cd /home/mi/feishu-bridge && python3 -m pytest tests/test_config.py tests/test_cards.py -x -v
</verification>

<success_criteria>
- Config model extended with 3 new fields, all with correct defaults
- requirements.txt includes claude-agent-sdk==0.1.53
- .env.example documents new env vars
- update_card_content() and send_error_card() implemented and tested
- All existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/02-claude-integration/02-01-SUMMARY.md`
</output>
